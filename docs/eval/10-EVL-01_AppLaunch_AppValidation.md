# EVAL-01: App Launch & Validation Test Specification

This document defines the comprehensive test matrix, acceptance criteria, and traceability mapping for validating the startup and initialization flow (`SEQ-01` / `REQ-01`).
Tests are structured across three test levels: **Desktop App Level (E2E / Integration)**, **React Level (Frontend Unit/Component)**, and **Tauri Level (Rust Unit/IPC)**.

---

## 1. Desktop App Level Tests (E2E / System Integration)

These integration tests verify the end-to-end behavior of the packaged Desktop Application, including multi-process double-booting (multiple workspace instances) and host OS interactions.

| Test ID | Trace Requirement ID | Test Type | Test Scenario | Test Steps | Expected Result |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-01-E2E-001** | `REQ-01-RULE-001`<br/>`REQ-01-FUNC-101`<br/>`REQ-01-FUNC-201`<br/>`REQ-01-FUNC-301`<br/>`REQ-01-FUNC-306`<br/>`REQ-01-FUNC-501`<br/>`REQ-01-FUNC-502` | Positive (Normal) | Normal Direct Executable Launch | 1. Double-click `hasm-app.exe` directly (no CLI arguments).<br/>2. Observe initialization sequence and screen transition. | 1. App mounts with initial splash/loading state (`isLoading = true`).<br/>2. Passes Check 1 and Check 2 (`isModelSelected = false`).<br/>3. Navigates to `/select` page safely with `Submit` button disabled. |
| **TC-01-E2E-002** | `REQ-01-RULE-001`<br/>`REQ-01-FUNC-304`<br/>`REQ-01-FUNC-305`<br/>`REQ-01-FUNC-401`<br/>`REQ-01-FUNC-402`<br/>`REQ-01-FUNC-406`<br/>`REQ-01-FUNC-601`<br/>`REQ-01-FUNC-602` | Positive (Normal) | Context Menu Launch with Valid Path | 1. Right-click a valid HASM folder in File Explorer.<br/>2. Select "Open with HASM App". | 1. App boots and extracts path via CLI arguments.<br/>2. Passes Check 3 path verification (`Path::exists() == true`).<br/>3. Bypasses `/select` page directly and navigates to `/loading-model` with `modelPath` in route state. |
| **TC-01-E2E-003** | `REQ-01-FUNC-503`<br/>`REQ-01-FUNC-506`<br/>`REQ-01-FUNC-510`<br/>`REQ-01-FUNC-511`<br/>`REQ-01-FUNC-601`<br/>`REQ-01-FUNC-602` | Positive (Normal) | Manual Path Input & Form Submission | 1. Launch app directly to reach `/select` page.<br/>2. Enter a valid local HASM folder path into the input field.<br/>3. Click the enabled `Submit` button. | 1. Real-time debounced validation succeeds.<br/>2. `Submit` button becomes enabled.<br/>3. Clicking `Submit` captures `modelPath`, sets `isLoading = true`, and navigates to `/loading-model`. |
| **TC-01-E2E-004** | `REQ-01-RULE-006` | Concurrency (Multi-Process) | Multi-Process Double-Booting (Multiple Workspaces) | 1. Launch Application Instance A with HASM Folder A.<br/>2. Simultaneously launch Application Instance B with HASM Folder B. | 1. OS launches two completely independent processes.<br/>2. Instance A holds Folder A in its memory space; Instance B holds Folder B without cross-process state contamination. |
| **TC-01-E2E-005** | `REQ-01-RULE-002`<br/>`REQ-01-FUNC-204`<br/>`REQ-01-FUNC-205` | Negative (Reject / Err) | Missing External Markdown App Executable | 1. Delete or rename the external HASM Markdown App executable on OS.<br/>2. Launch `hasm-app.exe`. | 1. Check 1 fails during environment check.<br/>2. Subsequent checks are immediately aborted.<br/>3. App Router navigates to `/error-app` and displays error description. |
| **TC-01-E2E-006** | `REQ-01-RULE-003`<br/>`REQ-01-FUNC-404`<br/>`REQ-01-FUNC-405`<br/>`REQ-01-FUNC-501`<br/>`REQ-01-FUNC-502` | Negative (Reject / Err) | Context Menu Launch with Non-Existent Path (Deleted Workspace) | 1. Launch via context menu using a target folder path.<br/>2. Delete or rename the target folder in OS right before launch. | 1. Check 3 fails at disk path existence check.<br/>2. Does NOT navigate to `/error-app`.<br/>3. Sets `error` state, resets `isModelSelected = false`, and gracefully falls back to `/select` page. |
| **TC-01-E2E-007** | `REQ-01-RULE-005`<br/>`REQ-01-FUNC-505`<br/>`REQ-01-FUNC-508`<br/>`REQ-01-FUNC-509` | Negative (Timeout) | Unresponsive Network / NAS Path Input | 1. Open `/select` page.<br/>2. Input an unroutable network UNC path (e.g. `\\192.168.255.255\share`). | 1. Real-time IPC is triggered.<br/>2. OS FS check blocks, but frontend enforces **2,000ms** timeout.<br/>3. `Submit` button remains disabled and an inline timeout warning is displayed without UI freeze. |
| **TC-01-E2E-008** | `REQ-01-RULE-001`<br/>`REQ-01-FUNC-303`<br/>`REQ-01-FUNC-405` | Negative (Security / Injection) | Malicious Command / Path Injection via CLI | 1. Execute `hasm-app.exe "C:\valid; rm -rf /"` or relative traversal path `..\..\..\System32`. | 1. Rust treats input purely as a literal string for `Path::new()`.<br/>2. Path check fails or rejects safely.<br/>3. App resets `isModelSelected = false` and falls back to `/select` page without shell execution or crash. |

---

## 2. React Level Tests (Frontend Component & State Logic)

These unit and integration tests focus on React hooks, UI component rendering, debouncing, state transitions, and React Router behavior using Vitest / React Testing Library.

| Test ID | Trace Requirement ID | Test Type | Component / Hook | Test Steps | Expected Result |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-01-REACT-001** | `REQ-01-FUNC-101`<br/>`REQ-01-FUNC-102`<br/>`REQ-01-FUNC-103` | Positive (Normal) | `App.tsx` (Mounting) | 1. Mount `App` component in test environment.<br/>2. Inspect initial state values before any IPC resolves. | 1. Loading UI is visible.<br/>2. `isLoading == true`, `loadState == 0`, `error == null`. |
| **TC-01-REACT-002** | `REQ-01-FUNC-201`<br/>`REQ-01-FUNC-206`<br/>`REQ-01-FUNC-301`<br/>`REQ-01-FUNC-309`<br/>`REQ-01-FUNC-310`<br/>`REQ-01-FUNC-406` | Positive (Normal) | Initialization Hook (Progress Flow) | 1. Mock IPC responses for Check 1, Check 2, and Check 3 to immediately resolve `Ok`.<br/>2. Trigger initialization sequence. | 1. `loadState` transitions sequentially from `0` → `1` → `2` → `3`.<br/>2. `modelPath` is successfully stored in React state. |
| **TC-01-REACT-003** | `REQ-01-FUNC-504` | Positive (Normal) | `SelectPage.tsx` (Debounce) | 1. Mount `SelectPage`.<br/>2. Simulate rapid input typing (10 keystrokes within 100ms). | 1. `invoke('validate_hasm_folder_path')` is NOT called immediately per keystroke.<br/>2. Exactly one IPC invocation occurs 300ms after typing stops. |
| **TC-01-REACT-004** | `REQ-01-FUNC-506`<br/>`REQ-01-FUNC-507` | Positive (Normal) | `SelectPage.tsx` (Validation Success UI) | 1. Mock `validate_hasm_folder_path` to resolve `Ok(())` within 2,000ms.<br/>2. Input text into the form. | 1. Form `Submit` button becomes enabled (`disabled == false`).<br/>2. Any active inline warning messages are removed from DOM. |
| **TC-01-REACT-005** | `REQ-01-FUNC-510`<br/>`REQ-01-FUNC-511`<br/>`REQ-01-FUNC-601`<br/>`REQ-01-FUNC-602` | Positive (Normal) | `SelectPage.tsx` (Submit & Routing) | 1. Input valid path and click enabled `Submit` button.<br/>2. Inspect state and router calls. | 1. Current `inputPath` is captured as `modelPath`.<br/>2. `isLoading` switches to `true`.<br/>3. React Router calls `navigate('/loading-model')` with `{ state: { path: modelPath } }`. |
| **TC-01-REACT-006** | `REQ-01-RULE-005`<br/>`REQ-01-FUNC-203`<br/>`REQ-01-FUNC-204`<br/>`REQ-01-FUNC-205` | Negative (Timeout) | Initialization Hook (Check 1 Timeout) | 1. Mock `validate_hasm_markdown_app` to never resolve (infinite promise).<br/>2. Advance mock timer past 5,000ms. | 1. Frontend 5,000ms timeout triggers.<br/>2. `isLoading` sets to `false`.<br/>3. `error` state receives "IPC call timed out" error.<br/>4. Router navigates to `/error-app`. |
| **TC-01-REACT-007** | `REQ-01-RULE-002`<br/>`REQ-01-FUNC-204`<br/>`REQ-01-FUNC-205` | Negative (Reject / Err) | Initialization Hook (Check 1 Reject / Err) | 1. Mock `validate_hasm_markdown_app` to immediately reject with `AppValidationError { code: "ERR_MARKDOWN_APP_INVALID" }`.<br/>2. Trigger initialization. | 1. `isLoading` sets to `false`.<br/>2. `error` state receives error message.<br/>3. Router navigates to `/error-app` without waiting for timeout. |
| **TC-01-REACT-008** | `REQ-01-FUNC-307`<br/>`REQ-01-FUNC-308` | Negative (Reject / Err) | Initialization Hook (Check 2 Reject / Err) | 1. Mock Check 1 to resolve `Ok(())`.<br/>2. Mock `validate_app_version` to reject with `AppValidationError { code: "ERR_VERSION_CHECK_FAILED" }`. | 1. `isLoading` sets to `false`.<br/>2. `error` state receives error message.<br/>3. Router navigates to `/error-app`. |
| **TC-01-REACT-009** | `REQ-01-RULE-005`<br/>`REQ-01-FUNC-403`<br/>`REQ-01-FUNC-404`<br/>`REQ-01-FUNC-405`<br/>`REQ-01-FUNC-501`<br/>`REQ-01-FUNC-502` | Negative (Timeout) | Initialization Hook (Check 3 Timeout) | 1. Mock Check 1 & 2 to resolve `Ok` (`isModelSelected = true`).<br/>2. Mock `validate_hasm_folder_path` for Check 3 to never resolve.<br/>3. Advance mock timer past 3,000ms. | 1. Frontend 3,000ms timeout triggers.<br/>2. `error` state receives "Specified HASM path does not exist".<br/>3. `isModelSelected` resets to `false`.<br/>4. `isLoading` sets to `false` and router navigates to `/select`. |
| **TC-01-REACT-010** | `REQ-01-RULE-003`<br/>`REQ-01-FUNC-404`<br/>`REQ-01-FUNC-405`<br/>`REQ-01-FUNC-501`<br/>`REQ-01-FUNC-502` | Negative (Reject / Err) | Initialization Hook (Check 3 Reject / Err) | 1. Mock Check 1 & 2 to resolve `Ok` (`isModelSelected = true`).<br/>2. Mock `validate_hasm_folder_path` for Check 3 to immediately reject with `ERR_TARGET_PATH_NOT_FOUND`. | 1. `error` state receives "Specified HASM path does not exist".<br/>2. `isModelSelected` resets to `false`.<br/>3. `isLoading` sets to `false` and router navigates to `/select` without delay. |
| **TC-01-REACT-011** | `REQ-01-FUNC-508`<br/>`REQ-01-FUNC-509` | Negative (Reject / Err) | `SelectPage.tsx` (Validation Reject / Err UI) | 1. Mock `validate_hasm_folder_path` to reject with `ERR_TARGET_PATH_NOT_FOUND`.<br/>2. Input invalid text into the form and wait 300ms debounce. | 1. Form `Submit` button remains disabled (`disabled == true`).<br/>2. Inline error message is displayed in the DOM. |
| **TC-01-REACT-012** | `REQ-01-RULE-005`<br/>`REQ-01-FUNC-505`<br/>`REQ-01-FUNC-508`<br/>`REQ-01-FUNC-509` | Negative (Timeout) | `SelectPage.tsx` (Validation Timeout UI) | 1. Mock `validate_hasm_folder_path` to never resolve.<br/>2. Input text into the form, wait 300ms debounce, and advance timer past 2,000ms. | 1. Real-time validation times out at 2,000ms.<br/>2. Form `Submit` button remains disabled (`disabled == true`).<br/>3. Inline warning "Path verification timed out" is displayed in the DOM. |
| **TC-01-REACT-013** | `REQ-01-FUNC-501`<br/>`REQ-01-FUNC-511` | Negative (Stress / Event Storming) | `SelectPage.tsx` (Double Click Event Storming) | 1. Input valid path to enable `Submit` button.<br/>2. Rapidly click `Submit` button 5 times within 100ms. | 1. First click immediately disables button and sets `isLoading = true`.<br/>2. Subsequent 4 clicks are ignored.<br/>3. Router `navigate` is called exactly once. |

---

## 3. Tauri Level Tests (Rust Command & IPC Contracts)

These unit and integration tests verify the Rust backend functions, CLI parsing logic, and IPC data serialization/deserialization contracts using `cargo test`.

| Test ID | Trace Requirement ID | Test Type | Rust Module / Function | Test Steps | Expected Result |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-01-RUST-001** | `REQ-01-DATA-001`<br/>`REQ-01-DATA-002` | Positive (Normal) | `commands::types` | 1. Instantiate `AppValidationError` and `AppVersionResponse` structs.<br/>2. Serialize to JSON and deserialize back to struct. | 1. JSON serialization produces exact expected keys (`code`, `message`, `is_model_selected`, `path`, `version`).<br/>2. Deserialization succeeds without field loss. |
| **TC-01-RUST-002** | `REQ-01-FUNC-202` | Positive (Normal) | `validate_hasm_markdown_app` | 1. Execute `validate_hasm_markdown_app()` command in an environment with the executable present. | 1. Returns `Ok(())`. |
| **TC-01-RUST-003** | `REQ-01-FUNC-302`<br/>`REQ-01-FUNC-303`<br/>`REQ-01-FUNC-306` | Positive (Normal) | `validate_app_version` (Direct) | 1. Invoke `validate_app_version()` with mock `std::env::args()` containing no extra arguments. | 1. Returns `Ok(AppVersionResponse)`.<br/>2. `is_model_selected == false`, `path == None`, `version == CARGO_PKG_VERSION`. |
| **TC-01-RUST-004** | `REQ-01-FUNC-304`<br/>`REQ-01-FUNC-305` | Positive (Normal) | `validate_app_version` (CLI Arg) | 1. Invoke `validate_app_version()` with mock `std::env::args()` containing `["app.exe", "C:/test/path"]`. | 1. Returns `Ok(AppVersionResponse)`.<br/>2. `is_model_selected == true`, `path == Some("C:/test/path")`. |
| **TC-01-RUST-005** | `REQ-01-FUNC-402`<br/>`REQ-01-FUNC-503` | Positive (Normal) | `validate_hasm_folder_path` | 1. Call `validate_hasm_folder_path("C:/existing_temp_dir")`. | 1. Existing directory returns `Ok(())`. |
| **TC-01-RUST-006** | `REQ-01-FUNC-202` | Negative (Reject / Err) | `validate_hasm_markdown_app` (Missing) | 1. Execute `validate_hasm_markdown_app()` when the target binary is missing. | 1. Returns `Err(AppValidationError)` with code `ERR_MARKDOWN_APP_INVALID`. |
| **TC-01-RUST-007** | `REQ-01-FUNC-402`<br/>`REQ-01-FUNC-503` | Negative (Reject / Err) | `validate_hasm_folder_path` (Non-existent) | 1. Call `validate_hasm_folder_path("C:/invalid_path_xyz")`. | 1. Non-existent directory returns `Err(AppValidationError)` with `ERR_TARGET_PATH_NOT_FOUND`. |
| **TC-01-RUST-008** | `REQ-01-FUNC-402` | Negative (Security / Injection) | `validate_hasm_folder_path` (Security) | 1. Pass malformed/null-byte paths (e.g. `C:\path\0bad`). | 1. Function handles invalid string input safely.<br/>2. Returns `Err(AppValidationError)` without panic or unhandled exception. |